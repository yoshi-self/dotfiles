snippet if
    if (${0}) {
    }

snippet elseif
alias elif
    else if (${0}) {
    }

snippet else
    else {
      ${0}
    }

snippet for
    for (${0}) {
    }

snippet foreach
    foreach (${0}) {
    }
